This directory exists to support Streamlit Cloud dependency resolution when the
configured app path contains spaces and the builder incorrectly looks for
`Migration/requirements.txt` and `Migration/packages.txt`.
